import { JoinSearchWordsPipePipe } from './join-search-words-pipe.pipe';

describe('JoinSearchWordsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new JoinSearchWordsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
